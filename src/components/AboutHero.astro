---
import Heading from "./Heading.astro";


---

<section class="hero bg-dark">
    <div class="container-md">
        <div class="column column-main flex-1">
            <div class="content-md">
                <Heading tag="h1" size="xxl" fontFamily="primary" marginBottom="l">About Us</Heading>
                <p>
                    We're a small team doing big things for other small teams — schools, churches, businesses, breweries, artists, fundraisers, and anyone who wants to wear their message with pride.
                </p>
            </div>
        </div>
    </div>
    <div id="blob"></div>
    <div id="blur"></div>
</section>

<style lang="scss">
.hero {
    overflow: hidden; 
    position: relative;
    padding-top: 124px;

    h1, h2, h3, h4, p {
        color: var(--light-200)
    }

    .img-wrapper {
        width: 100%;
        border-radius: var(--border-radius-medium);
        overflow: hidden;
        height: 100%;
        aspect-ratio: 16 / 9;
    }

    .column {
        position: relative;
        z-index: 999;
    }
}

</style>