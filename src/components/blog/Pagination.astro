---
import Links from '../Links.astro';

type Props = {
    prev?: string;
    next?: string;
};

const { prev, next } = Astro.props;
---

<div class="pagination">
    {prev && <Links href={prev}>Previous</Links>}
    {next && <Links href={next}>Next</Links>}
</div>